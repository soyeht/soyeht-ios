[package]
name = "nat-probe-ffi"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[lib]
name = "nat_probe_ffi"
path = "src/lib.rs"
crate-type = ["lib", "staticlib", "cdylib"]

[dependencies]
nat-probe-rs = { path = ".vendor/theyos/admin/rust/nat-probe-rs" }
thiserror = "1"
serde_json = "1"
uniffi = { version = "0.31.2" }
