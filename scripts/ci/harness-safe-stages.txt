# Closed allowlist of TEST-HARNESS diagnostic tokens — DISTINCT provenance from
# engine-safe-stages.txt. These are emitted by EngineHarness itself, not by the
# engine: a static case ID per harness test, plus a static classification of the
# BootstrapInitializeClient.initialize transport outcome. They are appended as
# TWO synthetic JSON events to the captured engine.log so engine stages and
# harness classification flow through the SAME extractor/allowlist and correlate
# per capture. Every token is a fixed compile-time literal in the test target;
# none carries a value (no status code, body, header, userInfo, name, path, or
# UUID). Membership is EXACT. Blank lines and lines starting with '#' are
# ignored.
#
# ── static case IDs (which harness test produced the capture) ──
harness_case.status_only
harness_case.initialize_pair
harness_case.long_poll
# ── static BootstrapInitializeClient.initialize transport outcome classes ──
# (observed at the injected transport, upstream of BootstrapWire's networkDrop
# collapse; a category only, never a code/status/body/header/userInfo value)
harness_initialize.not_observed
harness_initialize.transport_returned
harness_initialize.transport_timed_out
harness_initialize.transport_connection_lost
harness_initialize.transport_cannot_connect
harness_initialize.transport_bad_server_response
harness_initialize.transport_other
