import SwiftUI

// Welcome mode: local engine state exists — resume/recover flow.
struct RecoverView: View {
    let onRecovered: () -> Void

    var body: some View {
        Text(verbatim: "TODO: RecoverView")
    }
}
