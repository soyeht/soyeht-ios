import SwiftUI

// Phase 3 US5 (stub): Discovered existing casa on Tailnet — auto-join flow.
struct AutoJoinView: View {
    let onJoined: () -> Void

    var body: some View {
        Text(verbatim: "TODO: AutoJoinView")
    }
}
